# Constants
  set $mod Mod4

# Window border multiplier.
  set $borders    1

# Number of pixels to resize by in resize mode.
  set $resize     2


set $workspace1 0 web
set $workspace2 1 sh
set $workspace3 2 app
set $workspace4 3 mail
set $workspace5 4 sh2
set $workspace6 5 sh3
set $workspace7 6 sh4
set $workspace8 7 sh5
set $workspace9 8 sh6
set $workspace10 9 game


# General Configuration
  font pango:Monaco 10
  floating_modifier $mod
  #hide_edge_borders both
  #new_window none
  #smart_borders on
  gaps inner 12
  gaps outer 8
  new_float	pixel 	2
  new_window    pixel 	2

# Window-Related Bindings
  bindsym $mod+x kill
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Shift+h move left
  bindsym $mod+Shift+j move down
  bindsym $mod+Shift+k move up
  bindsym $mod+Shift+l move right
  bindsym $mod+w floating toggle
  bindsym $mod+f fullscreen toggle
  #bindsym $mod+v layout toggle split
  #bindsym $mod+h split h
  #bindsym $mod+space focus mode_toggle

  #bindsym $mod+r mode "  "
  #mode "  " {
  #bindsym h resize shrink width 10 px or 10 ppt
  #bindsym j resize grow height 10 px or 10 ppt
  #bindsym k resize shrink height 10 px or 10 ppt
  #bindsym l resize grow width 10 px or 10 ppt
  #bindsym Escape mode "default"
  #}

# Restart-Related Bindings
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart

# Program-Related Bindings
  bindsym $mod+space 	  exec termite
  bindsym $mod+g          exec chromium

  bindsym $mod+r exec rofi -show run -lines 3 -eh 2 -width 100 -padding 300  -opacity "100" -bw 0 -bc "#80CBC4" -bg "#2a2337" -fg "$text-color" -hlbg "#80CBC4" -hlfg "#2a2337" -font "Monaco 20"
  bindsym --release Print exec imgur-screenshot
  bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"



# Volume & Media Bindings
  bindsym XF86AudioLowerVolume exec --no-startup-id pulseaudio-ctl down
  bindsym XF86AudioRaiseVolume exec --no-startup-id pulseaudio-ctl up
  bindsym XF86AudioMute exec --no-startup-id pulseaudio-ctl mute
  bindsym XF86AudioPlay exec playerctl play-pause
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

# Startup Programs
  exec --no-startup-id nm-applet
  exec --no-startup-id ibus-daemon
  #exec_always --no-startup-id feh --bg-scale ~/.wallpaper.jpg

# Workspace-Related Bindings
  bindsym $mod+1 workspace $workspace1
  bindsym $mod+2 workspace $workspace2
  bindsym $mod+3 workspace $workspace3
  bindsym $mod+4 workspace $workspace4
  bindsym $mod+5 workspace $workspace5

  #bindsym $mod+. workspace next|prev|next_on_output|prev_on_output

  bindsym $mod+Shift+1 move container to workspace $workspace1
  bindsym $mod+Shift+2 move container to workspace $workspace2
  bindsym $mod+Shift+3 move container to workspace $workspace3
  bindsym $mod+Shift+4 move container to workspace $workspace4
  bindsym $mod+Shift+5 move container to workspace $workspace5
  bindsym $mod+Shift+6 move container to workspace $workspace6
  bindsym $mod+Shift+7 move container to workspace $workspace7
  bindsym $mod+Shift+8 move container to workspace $workspace8
  bindsym $mod+Shift+9 move container to workspace $workspace9
  bindsym $mod+Shift+0 move container to workspace $workspace10

# Workspace Monitors
  #workspace $workspace1 output $monitor1
  #workspace $workspace2 output $monitor2

# Program Workspaces
  assign [class="termite"] $workspace2
  assign [class="chromium"] $workspace1

# Floating
  #for_window [class="Lxappearance"] floating enable

# Window colours:

   client.focused              		#8ec07c #8ec07c #ebdbb2 #12fee7
   client.focused_inactive     	#928374 #928374 #ebdbb2 #C2CBFF
   client.unfocused            		#928374 #928374 #ebdbb2 #C2CBFF
   client.urgent               		#F97EC6 #F97EC6 #ee0000 #F97EC6

bar {
  i3bar_command ~/.config/i3/lemonbar/i3_lemonbar.sh
}

# #Bar  
#   bar {
#     strip_workspace_numbers no
#     position top
#     height 40
#     status_command lemonbar
#     # status_command i3blocks -c ~/.config/i3/i3blocks.conf
#     separator_symbol "|"
#     tray_output none
#     font pango:Monaco 8


#   colors {
#     background #282828
#     statusline #ebdbb2
#     separator #ffffff
#     						#border  	background 	text
#     focused_workspace 	#8ec07c	#3c3836 		#ebdbb2
#     active_workspace 		#928374 	#3c3836 		#ebdbb2
#     inactive_workspace 	#282828 	#282828 		#febdbb2
#     urgent_workspace 	#bf616a 	#F97EC6 		#febdbb2
#     }
#    }

exec --no-startup-id xinput set-prop "11" "libinput Tapping Enabled" 1
exec --no-startup-id xinput set-prop "11" "libinput Natural Scrolling Enabled" 1
